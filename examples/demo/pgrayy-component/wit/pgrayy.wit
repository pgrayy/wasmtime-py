package pgrayy:client@0.1.0;

world pgrayy-client {
    /// Pure compute exports
    export greet: func(name: string) -> string;
    export add: func(a: s32, b: s32) -> s32;

    /// I/O export (uses wasi:http internally via the wasi crate)
    export fetch: async func(url: string) -> string;
}
